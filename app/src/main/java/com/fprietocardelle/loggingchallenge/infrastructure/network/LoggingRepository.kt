package com.fprietocardelle.loggingchallenge.infrastructure.network

import javax.inject.Inject

/* This repository would be in charge of saving the logs in the device,
 or the network, or wherever we want the logs to be submitted. */
class LoggingRepository @Inject constructor() {
    fun submitLog() {
        return
    }
}